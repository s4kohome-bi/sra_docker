services:
  
  
  smart-receipt:
  
    build: .
    
    ports:
      - "5000:5000"
    
    env_file:
      - .env
  n8n:
    image: n8nio/n8n

    ports:
      - "5678:5678"

    volumes:
      - n8n_data:/home/node/.n8n
      - ./test-files:/home/node/.n8n-files

volumes:
  
  n8n_data: